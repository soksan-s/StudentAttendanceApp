<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:orientation="horizontal"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="16dp">

    <!-- Left: filters + list -->
    <LinearLayout android:layout_width="0dp" android:layout_weight="2" android:orientation="vertical" android:layout_height="wrap_content" >

        <!-- filter row (dropdown spinners) -->
        <LinearLayout android:layout_width="match_parent" android:orientation="horizontal" android:layout_height="wrap_content" android:weightSum="5">
            <Spinner android:id="@+id/spinner_faculty" android:layout_weight="1" android:layout_width="0dp" android:layout_height="48dp"/>
            <Spinner android:id="@+id/spinner_program" android:layout_weight="1" android:layout_width="0dp" android:layout_height="48dp"/>
            <Spinner android:id="@+id/spinner_section" android:layout_weight="1" android:layout_width="0dp" android:layout_height="48dp"/>
            <Spinner android:id="@+id/spinner_session" android:layout_weight="1" android:layout_width="0dp" android:layout_height="48dp"/>
            <Spinner android:id="@+id/spinner_course" android:layout_weight="1" android:layout_width="0dp" android:layout_height="48dp"/>
        </LinearLayout>

        <Button android:id="@+id/btn_search" android:text="Search" android:layout_width="wrap_content" android:layout_height="48dp" android:layout_marginTop="8dp"/>

        <!-- RecyclerView - student attendance table -->
        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/rv_students"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_weight="1"
            android:layout_marginTop="12dp"/>
    </LinearLayout>

    <!-- Right: calendar widget -->
    <LinearLayout android:layout_width="0dp" android:layout_weight="1"
        android:layout_height="match_parent" android:orientation="vertical" android:paddingLeft="12dp">
        <TextView android:layout_height="match_parent" android:layout_width="match_parent" android:text="Jan 2023" android:textStyle="bold" android:layout_marginBottom="8dp"/>
        <View android:layout_width="match_parent" android:layout_height="wrap_content" android:background="@color/lightGray"/>
        <!-- Placeholder for calendar view. You can substitute with Material Calendar or third party calendar -->
    </LinearLayout>
</LinearLayout>
